import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

const AppNavigator: React.FC = () => {
  return (
    <NavigationContainer>
      {/* Navigation structure will be implemented in later tasks */}
    </NavigationContainer>
  );
};

export default AppNavigator;
